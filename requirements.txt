# --- core RAG ---
sentence-transformers>=3.0.0     # local embeddings + cross-encoder reranker
faiss-cpu>=1.8.0                 # sub-millisecond vector search
bm25s>=0.2.0                     # lexical (BM25); sparse-matrix backed.
                                 # NOT rank-bm25: pure Python, degrades
                                 # superlinearly -- 63.75ms vs 0.06ms per query
                                 # at 103k chunks (652x), 99% of retrieval time.
datasets>=2.19.0                 # streaming the MSMARCO-XI subset from HF
numpy>=1.26.0

# --- voice ---
sarvamai>=0.1.0                  # Sarvam STT (speech-to-text)

# --- harness / api ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9          # file uploads (audio) in FastAPI
pydantic>=2.6.0
tenacity>=8.2.0                  # retries with backoff for network stages
python-dotenv>=1.0.0
